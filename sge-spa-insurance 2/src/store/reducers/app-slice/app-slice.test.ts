import { setLopdp, loadValues, reset } from './app-slice'

// todo esto aun debe ser implementado
describe('setLopdp', () => {
  it("should return ''", () => {
    //const result = setLopdp()
    const result = () => {
      return 'value1'
    }

    expect(result).toEqual('value')
  })
})

// todo esto aun debe ser implementado
describe('loadValues', () => {
  it("should return ''", () => {
    //const result = loadValues()
    const result = () => {
      return 'value1'
    }

    expect(result).toEqual('value')
  })
})

// todo esto aun debe ser implementado
describe('reset', () => {
  it("should return ''", () => {
    //const result = reset()
    const result = () => {
      return 'value1'
    }

    expect(result).toEqual('value')
  })
})
