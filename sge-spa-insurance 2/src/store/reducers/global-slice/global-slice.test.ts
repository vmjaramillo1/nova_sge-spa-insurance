import { authenticate, setError, setPortalHub } from './global-slice'

// todo esto aun debe ser implementado
describe('authenticate', () => {
  it("should return ''", () => {
    //const result = authenticate()
    const result = () => {
      return 'value1'
    }

    expect(result).toEqual('value')
  })
})

// todo esto aun debe ser implementado
describe('setError', () => {
  it("should return ''", () => {
    //const result = setError()
    const result = () => {
      return 'value1'
    }

    expect(result).toEqual('value')
  })
})

// todo esto aun debe ser implementado
describe('setPortalHub', () => {
  it("should return ''", () => {
    //const result = setPortalHub()
    const result = () => {
      return 'value1'
    }

    expect(result).toEqual('value')
  })
})
